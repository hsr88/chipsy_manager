# Netlify configuration for Chipsy Top Manager

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for hash-based routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
